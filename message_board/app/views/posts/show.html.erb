<p id="notice"><%= notice %></p>

<h3>
  <%= @post.title %>
</h3>
<small>
  posted by: <b>
    <%= @post.user.email %>
  </b><br>
  on <%= @post.created_at.to_formatted_s(:long) %>
</small>
<p>
  <br>
  <%= @post.content %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
